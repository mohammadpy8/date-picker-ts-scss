type TYearsData = {
  id: number;
  year: string;
}[];

type TMonthsData = {
  value: number;
  label: string;
}[];

type TDayData = {
  id: number;
  day: string;
}[];

export type { TYearsData, TMonthsData, TDayData };
